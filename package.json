{
  "name": "crawllumav",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm i express nodemon node-fetch@2 concurrently && node cache.js",
    "start": "concurrently \"nodemon --watch . --ext js app.js\" \"php -S localhost:3000 -t ./build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0",
    "node-fetch": "^2.7.0",
    "nodemon": "^3.1.7"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
